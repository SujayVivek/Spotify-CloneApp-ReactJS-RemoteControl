import React, {
    createContext,
    useContext,
    useReducer} from "react"

    export const StateContext = createContext();//prepares the data, and we need to create the data there!

    export const DataLayer = ({})//continue from here later ONNNNN